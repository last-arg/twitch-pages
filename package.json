{
  "name": "twitch-pages",
  "version": "1.0.0",
  "description": "Twitch alternative page with htmx and alpine",
  "scripts": {
    "netlify": "netlify dev",
    "dev": "vite",
    "css": "npx unocss 'src/**/*.webc' 'netlify/functions/*.{js,ts}' 'public/partials/*.html' 'src/*.ts' -o public/css/main.css",
    "build": "npm run css && tsc && vite build",
    "serve": "vite preview",
    "watch:js": "npx esbuild src/main.ts --bundle --outdir=public/js/ --watch"
  },
  "dependencies": {
    "@alpinejs/persist": "^3.2.4",
    "alpinejs": "^3.2.4",
    "htmx.org": "^1.6.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.16",
    "@11ty/eleventy-plugin-webc": "^0.5.1",
    "@leanadmin/alpine-typescript": "^0.1.25",
    "@netlify/functions": "^0.8.0",
    "@types/node": "^16.11.6",
    "@unocss/cli": "^0.46.1",
    "@unocss/reset": "^0.46.1",
    "netlify-cli": "^6.14.19",
    "node-fetch": "^2.6.6",
    "typescript": "^4.3.2",
    "unocss": "^0.46.1"
  }
}
