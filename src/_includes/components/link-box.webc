<div webc:root webc:keep class="flex-grow">
  <script webc:type="js" webc:is="template">
    const valid_names = ["category", "user-videos"]
    if (!valid_names.includes(this.name)) {
      throw new Error(`Invalid 'name' attribute '${this.name}'. Valid values: ${valid_names.join(", ")}`)
    }
    const name = this.name;
    const arg_name = this.arg_name;
    const path = this.global.paths[name];
    const href_url = `'${path.url}'.replace(':${name}', ${arg_name})`;
    const click_fn = `clickSidebar('${name}', $el.getAttribute('href'))`;
    const configs = {
      "category": {
        width: this.global.config.image.category.width,
        height: this.global.config.image.category.height,
        bind_src: `getImageSrc(${arg_name.split(".")[0]}.box_art_url)`,
      },
      "user-videos": {
        width: this.global.config.image.user.width,
        height: this.global.config.image.user.height,
        bind_src: "$store.profile_images.imgUrl(stream.user_id)",
      }
    };
    const config = configs[name];
    `
      <a class="link-box"
        data-card="${name}"
        href="#" hx-push-url="#"
        x-init="
          href_url = ${href_url};
          $el.setAttribute('href', href_url);
          $el.setAttribute('hx-push-url', href_url);
          htmx.process($el);
        "
        x-on:click="${click_fn}"
        hx-target="#main"
        hx-get="/public/partials/${name}.html"
      >
        <img x-bind:src="${config.bind_src}" alt="" width="${config.width}" height="${config.height}" />
        <div class="link-title">
          <slot></slot>
        </div>
      </a>
    `;
  </script>
</div>
<style webc:nokeep>
.link-box {
  display: flex;
  --card-bg: var(--gray-9);
  background: var(--card-bg);
  border-left: var(--space-3xs) solid var(--card-bg);
  padding-inline-start: var(--space-3xs);
  text-decoration: none;
  color: var(--gray-3);
  width: 100%;
}

.link-box img {
  width: 3.3rem;
}

.link-title {
  padding: var(--space-3xs) var(--space-2xs);
  font-size: var(--step--1);
  display: flex;
  flex-direction: column;
  justify-content: center;
  width: 100%;
}

.link-title p {
  display: -webkit-box;
  -webkit-box-orient: vertical;
  overflow: hidden;
}


.link-box:hover,
.link-box:focus {
  border-left-color: var(--violet-8);
  color: var(--gray-0);
}

.link-box[data-card=category] .link-title p {
  -webkit-line-clamp: 2;
}

.link-box[data-card=user-videos] .link-title p {
  -webkit-line-clamp: 1;
}

.link-box[data-card=user-videos] img {
  width: 4rem;
}
</style>
